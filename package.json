{
  "name": "Dendron",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "init": "npx dendron publish init",
    "serve-dev": "npx kill-port 3000 && npx dendron publish dev",
    "deploy": "npx kill-port 3000 && npx dendron publish export --target github",
    "kill-port3000": "npx kill-port 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@dendronhq/dendron-cli": "^0.122.0"
  }
}
